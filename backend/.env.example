# GameOn Backend Environment Configuration
# REQUIRED VARIABLES (must be set for production):
#   - MONGODB_URI: MongoDB Atlas connection string
#   - JWT_SECRET: Minimum 32 characters for JWT token signing
#   - NODE_ENV: Set to 'production' for Vercel/production deployments

# Database Connection - MongoDB Atlas
# IMPORTANT: Must be configured in Vercel environment settings
# For development, use local MongoDB or MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0

# Alternative name (for Render compatibility)
# VERCEL/PRODUCTION DEPLOYMENT: Only MONGODB_URI is used
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0

# Server Configuration
NODE_ENV=development
PORT=5000

# Security - JWT Secret (minimum 32 characters, use strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long

# JWT Refresh Secret
JWT_REFRESH_SECRET=your-refresh-token-secret-key-different-from-jwt-secret

# CORS Origins (comma-separated) - Production domains
CORS_ORIGIN=https://gameonesport.xyz,https://admin.gameonesport.xyz

# Frontend URL (for referral links, emails, redirects)
FRONTEND_URL=https://gameonesport.xyz

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# VERCEL PRODUCTION CHECKLIST:
# [ ] Set MONGODB_URI in Vercel project settingsit
# [ ] Set JWT_SECRET (use strong random value, not the example)
# [ ] Set NODE_ENV=production
# [ ] Verify database connectivity before deploying
# [ ] Test /api/health endpoint after deployment